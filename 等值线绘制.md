# Plotly.js 等值线（Contour）绘制原理详解

## 目录
1. [概述](#概述)
2. [核心算法：Marching Squares](#核心算法marching-squares)
3. [处理流程](#处理流程)
4. [关键模块解析](#关键模块解析)
5. [数据结构](#数据结构)
6. [渲染流程](#渲染流程)

---

## 概述

Plotly.js 的 contour 等值线绘制基于经典的 **Marching Squares** 算法。该算法通过将 2D 标量场（z 值网格）转换为等值线路径来实现等值线可视化。

### 核心文件结构

```
src/traces/contour/
├── index.js           # 模块入口
├── calc.js            # 计算入口（调用 heatmap calc）
├── plot.js            # 渲染入口
├── attributes.js      # 属性定义
├── set_contours.js    # 等值线级别计算
├── empty_pathinfo.js  # 初始化路径数据结构
├── make_crossings.js  # Marching Squares 核心算法
├── find_all_paths.js  # 路径追踪
└── close_boundaries.js # 边界闭合处理
```

---

## 核心算法：Marching Squares

### 算法原理

Marching Squares 是 Marching Cubes 的 2D 版本，用于从 2D 标量场中提取等值线。

**基本思想**：将网格划分为小的方形单元格，对于每个单元格，根据四个角点的值与等值线级别的关系，确定等值线穿过该单元格的方式。

### 索引编码（make_crossings.js:68-82）

```javascript
function getMarchingIndex(val, corners) {
    // corners: [[z00, z01], [z10, z11]]
    // 位编码：从左上角开始，顺时针方向
    var mi = (corners[0][0] > val ? 0 : 1) +   // 左上
             (corners[0][1] > val ? 0 : 2) +   // 右上
             (corners[1][1] > val ? 0 : 4) +   // 右下
             (corners[1][0] > val ? 0 : 8);    // 左下

    // 鞍点处理
    if(mi === 5 || mi === 10) {
        var avg = (corners[0][0] + corners[0][1] +
                   corners[1][0] + corners[1][1]) / 4;
        if(val > avg) return (mi === 5) ? 713 : 1114;  // 双峰
        return (mi === 5) ? 104 : 208;  // 双谷
    }
    return (mi === 15) ? 0 : mi;
}
```

### 16 种情况分类

| 索引 | 描述 | 索引 | 描述 |
|------|------|------|------|
| 0 | 全在等值线下方，无交点 | 8 | 仅左下角在上方 |
| 1 | 仅左上角在上方 | 9 | 左上+左下 |
| 2 | 仅右上角在上方 | 10 | 左上+右下（鞍点）|
| 3 | 左上+右上 | 11 | 右上+右下 |
| 4 | 仅右下角在上方 | 12 | 左上+右下 |
| 5 | 左上+右下（鞍点）| 13 | 左上+左下+右下 |
| 6 | 右上+右下 | 14 | 右上+左下+右下 |
| 7 | 左上+右上+右下 | 15 | 全在上方，无交点 |

### 鞍点消歧

鞍点（索引 5 和 10）存在两种可能的连接方式。Plotly 使用四角平均值与等值线级别的关系来确定连接方式：

```javascript
// constants.js 中定义
CHOOSESADDLE: {
    104: [4, 1],   // 双谷情况：根据方向选择索引 4 或 1
    208: [2, 8],
    713: [7, 13],  // 双峰情况
    1114: [11, 14]
}
```

---

## 处理流程

### 1. 计算阶段（calc.js）

```
heatmapCalc()
    ├─ 数据清洗和转换
    ├─ 空值检测和插值（interp2d）
    ├─ 坐标轴处理
    └─ 返回 calcdata

setContours()
    ├─ 处理自定义阈值（thresholds）
    ├─ 自动生成等值线级别（autocontour）
    └─ 计算 start, end, size

Colorscale.calc()
    └─ 计算颜色映射
```

### 2. 等值线路径计算（plot.js）

```
makeCrossings(pathinfo)
    └─ 为每个网格单元计算 marching index

findAllPaths(pathinfo)
    ├─ 从边界起点追踪路径
    ├─ 从内部交叉点追踪路径
    └─ 生成 edgepaths（开放路径）和 paths（闭合路径）

closeBoundaries(pathinfo)
    └─ 处理边界闭合，确定 prefixBoundary
```

### 3. 渲染阶段（plot.js）

```
makeBackground()
    └─ 绘制背景（fill 模式）

makeFills()
    └─ 绘制填充区域

makeLinesAndLabels()
    ├─ 绘制等值线
    └─ 添加标签

clipGaps()
    └─ 处理空值裁剪
```

---

## 关键模块解析

### 1. 空值插值（interp2d.js）

使用迭代泊松方程求解器填充缺失数据：

```javascript
// 核心思想：每个缺失点的值等于其有效邻居的平均值
function iterateInterp2d(z, emptyPoints, overshoot) {
    for(p = 0; p < emptyPoints.length; p++) {
        // 计算有效邻居的平均值
        z[i][j] = neighborSum / neighborCount;

        // 超调加速收敛
        z[i][j] = (1 + overshoot) * z[i][j] - overshoot * initialVal;
    }
}
```

### 2. 路径追踪（find_all_paths.js）

**关键函数**：`makePath(pi, startLoc, edgeflag, xtol, ytol)`

```javascript
function makePath(pi, loc, edgeflag, xtol, ytol) {
    // 1. 获取起始步进方向
    var marchStep = getStartStep(mi, edgeflag, loc);

    // 2. 向后追溯半步，获取第一个交点
    var pts = [getInterpPx(pi, loc, [-marchStep[0], -marchStep[1]])];

    // 3. 跟踪路径
    for(cnt = 0; cnt < 10000; cnt++) {
        // 使用 marching index 确定下一步方向
        marchStep = constants.NEWDELTA[mi];

        // 插值计算交点的精确像素位置
        pts.push(getInterpPx(pi, loc, marchStep));

        // 更新位置
        loc[0] += marchStep[0];
        loc[1] += marchStep[1];

        // 检查是否闭合或到达边界
        if(closedLoop || (edgeflag && atEdge)) break;
    }

    // 4. 路径简化：删除距离过近的点
    // 5. 分类为闭合路径或开放路径
}
```

### 3. 线性插值（find_all_paths.js:265-293）

为了获得更精确的交点位置，使用线性插值：

```javascript
function getInterpPx(pi, loc, step) {
    var zxy = pi.z[locy][locx];

    if(step[1]) {  // 水平方向插值
        var dx = (pi.level - zxy) / (pi.z[locy][locx + 1] - zxy);
        // 在对数空间中正确处理插值
        var dxl = (dx !== 1 ? (1 - dx) * xa.c2l(pi.x[locx]) : 0) +
                  (dx !== 0 ? dx * xa.c2l(pi.x[locx + 1]) : 0);
        return [xa.c2p(xa.l2c(dxl), true), ya.c2p(pi.y[locy], true),
                locx + dx, locy];
    } else {  // 垂直方向插值
        var dy = (pi.level - zxy) / (pi.z[locy + 1][locx] - zxy);
        // 类似处理
    }
}
```

### 4. 路径平滑（drawing/index.js:1219-1253）

使用 **Catmull-Rom 样条**进行路径平滑：

```javascript
drawing.smoothopen = function(pts, smoothness) {
    // 计算每个点的切线
    for(i = 1; i < pts.length - 1; i++) {
        tangents.push(makeTangent(pts[i - 1], pts[i], pts[i + 1], smoothness));
    }

    // 构建二次贝塞尔曲线序列
    path += 'Q' + tangents[0][0] + ' ' + pts[1];
    for(i = 2; i < pts.length - 1; i++) {
        path += 'C' + tangents[i - 2][1] + ' ' + tangents[i - 1][0] + ' ' + pts[i];
    }
    path += 'Q' + tangents[pts.length - 3][1] + ' ' + pts[pts.length - 1];
    return path;
};
```

---

## 数据结构

### pathinfo 结构（empty_pathinfo.js）

```javascript
pathinfo = [
    {
        level: 10,              // 当前等值线级别值
        z: [[...]],             // 原始 z 值网格
        x: [...],               // x 坐标数组
        y: [...],               // y 坐标数组
        xaxis: {...},           // x 轴对象
        yaxis: {...},           // y 轴对象
        crossings: {},          // 交叉点映射 'x,y' -> marchingIndex
        starts: [[x0,y0], ...], // 边界起点列表
        edgepaths: [...],       // 开放路径列表（接触边界）
        paths: [...],           // 闭合路径列表（完全在内部）
        smoothing: 0.5,         // 平滑系数
        prefixBoundary: false   // 是否需要前缀边界
    },
    // ... 每个等值线级别一个对象
]
```

### crossings 映射

```javascript
crossings = {
    "0,0": 1,      // 网格单元(0,0)的marching index为1
    "1,2": 6,
    // ...
}
```

---

## 渲染流程

### 1. 背景和填充

```javascript
function makeFills(plotgroup, pathinfo, perimeter, contours) {
    if(hasFills) {
        closeBoundaries(pathinfo, contours);
    }

    fillitems.each(function(pi) {
        // 构建完整填充路径
        var fullpath = (pi.prefixBoundary ? boundaryPath : '') +
                       joinAllPaths(pi, perimeter);
    });
}
```

**joinAllPaths** 负责将多个路径连接成一个完整的填充区域：
- 首先沿着边界追踪，连接所有开放路径
- 然后添加所有闭合路径
- 使用奇偶规则（evenodd）确保正确的填充

### 2. 线条绘制

```javascript
exports.createLines = function(lineContainer, makeLines, pathinfo, isStatic) {
    // 开放路径
    opencontourlines
        .attr('d', function(d) {
            return Drawing.smoothopen(d, smoothing);
        })

    // 闭合路径
    closedcontourlines
        .attr('d', function(d) {
            return Drawing.smoothclosed(d, smoothing);
        })
};
```

### 3. 标签放置（plot.js:447-481）

使用优化算法寻找最佳标签位置：

```javascript
exports.findBestTextLocation = function(path, pathBounds, textOpts, labelData, plotBounds) {
    // 1. 在路径上均匀采样多个候选位置
    for(var p = p0; p < pMax; p += dp) {
        var newLocation = Lib.getTextLocation(path, pathBounds.total, p, textWidth);
        var newCost = locationCost(newLocation, textOpts, labelData, plotBounds);
        if(newCost < cost) {
            cost = newCost;
            loc = newLocation;
        }
    }

    // 2. 在最佳位置附近进行二分搜索细化
    for(var j = 0; j < costConstants.ITERATIONS; j++) {
        dp /= 2;
        // 继续搜索...
    }
};
```

**成本函数**考虑：
- 边界距离（避免靠近边缘）
- 水平角度（标签应尽量水平）
- 邻近标签距离（避免重叠）

---

## 常量定义（constants.js）

```javascript
module.exports = {
    // 边界起点判断
    BOTTOMSTART: [1, 9, 13, 104, 713],
    TOPSTART: [4, 6, 7, 104, 713],
    LEFTSTART: [8, 12, 14, 208, 1114],
    RIGHTSTART: [2, 3, 11, 208, 1114],

    // 方向增量
    NEWDELTA: [
        null, [-1, 0], [0, -1], [-1, 0],
        [1, 0], null, [0, -1], [-1, 0],
        // ...
    ],

    // 标签配置
    LABELDISTANCE: 2,      // 标签间距（相对对角线）
    LABELMIN: 3,           // 最小路径长度（相对标签大小）
    LABELMAX: 10,          // 每条路径最大标签数
};
```

---

## 总结

Plotly.js 的 contour 实现是一个完整的等值线渲染系统，核心特点：

1. **Marching Squares 算法**：高效的等值线提取
2. **鞍点消歧**：使用四角平均值确定正确的连接方式
3. **线性插值**：精确计算交点位置，支持对数坐标轴
4. **路径简化**：自动合并过近的点
5. **Catmull-Rom 平滑**：可选的路径平滑
6. **智能标签布局**：基于成本函数的优化算法
7. **空值处理**：迭代泊松方程填充缺失数据

这个实现兼顾了正确性、性能和可扩展性，是 Web 等值线渲染的优秀参考实现。
